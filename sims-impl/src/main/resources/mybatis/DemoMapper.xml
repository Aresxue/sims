<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tydic.simscloud.mapper.DemoMapper">
    <select id="findStudentByName" parameterType="com.tydic.simscloud.entity.Student"
            resultType="com.tydic.simscloud.entity.Student">
        SELECT *
        FROM student
        WHERE student_name LIKE CONCAT('%',#{studentName},'%')
    </select>

    <!-- 新增一个学生信息-->
    <insert id="addStudent" parameterType="com.tydic.simscloud.entity.Student">
        INSERT INTO student(student_id, class_id, student_name, student_sex, student_age)
        VALUES (#{studentId}, #{classId}, #{studentName}, #{studentSex}, #{studentAge});
    </insert>
</mapper>

